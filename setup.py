from setuptools import setup

setup(
      name='detuned_excitation',
      version='0.0.1',
      install_requires=[
                        'numpy',
                        'matplotlib',
                        'tqdm'
                        ]
      )
